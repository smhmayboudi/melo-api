import { DataCacheEntityRepository } from "./data.cache.entity.repository";

describe("DataCacheEntityRepository", () => {
  it("should be defined", () => {
    expect(new DataCacheEntityRepository()).toBeDefined();
  });
});
