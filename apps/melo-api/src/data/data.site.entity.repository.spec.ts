import { DataSiteEntityRepository } from "./data.site.entity.repository";

describe("DataSiteEntityRepository", () => {
  it("should be defined", () => {
    expect(new DataSiteEntityRepository()).toBeDefined();
  });
});
